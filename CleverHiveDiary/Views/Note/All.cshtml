@using CleverHiveDiary.Core.ViewModels.Note
@model IEnumerable<NoteViewModel>

@{
    ViewBag.Title = "Бележки";
}

<h1 class="text-center">@ViewData["Title"]</h1>
@if (Model.Count() == 0)
{

    <h3 class="text-center">Няма бележки!</h3>
}
else
{
    @*<div class="text-center row">
        @foreach (var note in Model)
        {

            <div class="card-body">
                <h5 class="card-title mt-1">@note.Title</h5>
                <p class="mb-0">Заглавие</p>
            </div>
            <div class="card-body">
                <h5 class="card-title mt-1">@note.Text</h5>
                <p class="mb-0">Съдържание</p>
            </div>

            <form class="input-group-sm" asp-route-Id="@note.Id" asp-controller="Note" asp-action="Delete" method="post">
                <input type="submit" value="Премахни" class="fs-6 btn btn-success mb-3 p-2" />
            </form>
        }

    </div>*@
    <table class="table">
        <thead>
            <tr>
                <th>Заглавие</th>
                <th>Съдържание</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var note in Model)
            {
                <tr>
                    <td>@note.Title</td>
                    <td>@note.Text</td>
                    <td>
                        <a asp-area="" asp-controller="Note" asp-action="Edit" asp-route-noteId="@note.Id"
                           class="btn btn-warning">Промени</a>
                    </td>
                    <td>
                        <form asp-action="Delete" asp-route-id="@note.Id">
                            <button type="submit" class="btn btn-danger">Премахни</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

<a asp-area="" asp-controller="Note" asp-action="Add"
   class="btn btn-info">Добави</a>
                    @*</td>*@




